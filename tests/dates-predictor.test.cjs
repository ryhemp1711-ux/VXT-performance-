const {test}=require('node:test');const assert=require('node:assert/strict');const V=require('../model.js');
test('dates normalize US slash and ISO input to same canonical date',()=>{for(const d of ['9/2/2026','09/02/2026','2026-9-2','2026/09/02']){assert.equal(V.normalizeDate(d),'2026-09-02');assert.equal(V.formatDate(d),'09/02/2026');}});
test('season dates remain partial instead of inventing January 1',()=>{assert.equal(V.normalizeDate('--/--/2026'),'2026');assert.equal(V.formatDate('2026'),'--/--/2026');});
test('invalid calendar dates rejected including non-leap Feb 29',()=>{for(const d of ['02/29/2025','2026-02-30','13/01/2026','09/31/2026','garbage'])assert.throws(()=>V.normalizeDate(d));assert.equal(V.normalizeDate('2/29/2024'),'2024-02-29');});
test('backup normalization does not mutate input records',()=>{const d={version:1,athletes:[{id:'a',name:'A'}],results:[{id:'r',athleteId:'a',event:'100m',time:12,date:'9/2/2026'}],predictions:[]};const result=V.validateData(d);assert.equal(result.results[0].date,'2026-09-02');assert.equal(d.results[0].date,'9/2/2026');});
test('imported Unknown results available in default predictor selection, filters still apply',()=>{const rs=[{athleteId:'a',event:'100m',time:12,date:'2026',method:'Unknown'},{athleteId:'a',event:'100m',time:12.5,date:'2026-09-01',method:'Video'},{athleteId:'b',event:'100m',time:9.9,date:'2026',method:'FAT'}];assert.equal(V.savedBest(rs,'a','100m').time,12);assert.equal(V.savedBest(rs,'a','100m','Video').time,12.5);assert.equal(V.savedBest(rs,'a','Fly 20m'),null);});
